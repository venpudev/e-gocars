---
import Layout from "../layouts/Layout.astro";
import { Image } from "astro:assets";
import contactoHero from "../assets/contacto-hero.jpg";
---

<Layout 
  title="Contacto - Hablemos de tu Próximo Auto" 
  description="Contacta al equipo de E-GoCars."
>
  <main class="bg-gray-900 text-white">
    <section class="relative min-h-screen flex items-center justify-center py-16 md:py-24 px-4">
      <Image 
        src={contactoHero} 
        alt="Oficina moderna de E-GoCars" 
        class="absolute inset-0 w-full h-full object-cover -z-10 opacity-10"
      />
      <div class="max-w-6xl w-full mx-auto grid lg:grid-cols-2 gap-0 bg-gray-800 shadow-2xl rounded-2xl overflow-hidden border border-gray-700">
        <div class="p-8 md:p-12 flex flex-col">
          <div>
            <h1 class="text-4xl md:text-5xl font-bold mb-4">Estamos para Ayudarte</h1>
            <p class="text-lg text-gray-300 mb-10">Completa el formulario o utiliza nuestros canales directos.</p>
          </div>
          <div class="space-y-6 mt-4">
            <a href="/catalogo" class="flex items-center p-4 bg-gray-700/50 rounded-lg hover:bg-gray-700 transition-colors">
              <div class="w-10 h-10 flex items-center justify-center bg-amber-500 text-black rounded-md mr-4"><i class="fas fa-car"></i></div>
              <div>
                <h3 class="font-semibold text-white">Ver Catálogo</h3>
                <p class="text-sm text-gray-400">Explora nuestros vehículos.</p>
              </div>
            </a>
            <a href="https://wa.me/56989939362" target="_blank" class="flex items-center p-4 bg-gray-700/50 rounded-lg hover:bg-gray-700 transition-colors">
              <div class="w-10 h-10 flex items-center justify-center bg-amber-500 text-black rounded-md mr-4"><i class="fab fa-whatsapp"></i></div>
              <div>
                <h3 class="font-semibold text-white">Hablar por WhatsApp</h3>
                <p class="text-sm text-gray-400">Inicia una conversación.</p>
              </div>
            </a>
          </div>
        </div>
        <div class="bg-gray-900 p-8 md:p-12">
          <h2 class="text-3xl font-bold mb-8">Envíanos un Mensaje</h2>
          
          <form id="contact-form" class="space-y-5">
            <div class="space-y-2">
              <label for="name" class="text-sm font-medium text-gray-300">Nombre</label>
              <input type="text" id="name" name="name" required class="w-full px-4 py-3 bg-gray-800 border border-gray-700 rounded-lg focus:outline-none focus:ring-2 focus:ring-amber-500"/>
            </div>
            <div class="space-y-2">
              <label for="email" class="text-sm font-medium text-gray-300">Email</label>
              <input type="email" id="email" name="email" required class="w-full px-4 py-3 bg-gray-800 border border-gray-700 rounded-lg focus:outline-none focus:ring-2 focus:ring-amber-500"/>
            </div>
            <div class="space-y-2">
              <label for="phone" class="text-sm font-medium text-gray-300">Teléfono</label>
              <input type="tel" id="phone" name="phone" required class="w-full px-4 py-3 bg-gray-800 border border-gray-700 rounded-lg focus:outline-none focus:ring-2 focus:ring-amber-500"/>
            </div>
            <div class="space-y-2">
              <label for="message" class="text-sm font-medium text-gray-300">Mensaje</label>
              <textarea id="message" name="message" rows="4" required class="w-full px-4 py-3 bg-gray-800 border border-gray-700 rounded-lg focus:outline-none focus:ring-2 focus:ring-amber-500 resize-none"></textarea>
            </div>
            
            <div id="form-status" class="hidden" aria-live="polite"><p class="text-center font-medium p-3 rounded-lg"></p></div>

            <button type="submit" class="w-full bg-amber-500 hover:bg-amber-600 text-black font-bold py-3 px-6 rounded-lg transition-all duration-300 transform hover:scale-105 shadow-lg text-lg">
              Enviar Mensaje
            </button>
          </form>
        </div>
      </div>
    </section>
  </main>
</Layout>

<script>
  const form = document.getElementById("contact-form") as HTMLFormElement | null;
  const formStatus = document.getElementById("form-status") as HTMLDivElement | null;
  const statusText = formStatus?.querySelector("p") as HTMLParagraphElement | null;

  async function handleSubmit(event: Event) {
    event.preventDefault();
    if (!form || !formStatus || !statusText) return;

    const formData = new FormData(event.target as HTMLFormElement);
    const submitButton = form.querySelector('button[type="submit"]') as HTMLButtonElement | null;
    const originalButtonText = submitButton?.textContent || 'Enviar Mensaje';

    if (submitButton) {
        submitButton.disabled = true;
        submitButton.textContent = 'Enviando...';
    }

    try {
      const response = await fetch("https://formspree.io/f/xkgzdnnq", {
        method: "POST",
        body: formData,
        headers: {
            'Accept': 'application/json'
        }
      });

      if (response.ok) {
        statusText.textContent = "¡Gracias! Tu mensaje ha sido enviado.";
        formStatus.className = 'text-green-700 bg-green-100 border border-green-200 p-3 rounded-lg';
        formStatus.classList.remove('hidden');
        form.reset();
      } else {
        throw new Error('Error en la respuesta del servidor');
      }
    } catch (error) {
      statusText.textContent = "Hubo un error al enviar el mensaje. Intenta de nuevo.";
      formStatus.className = 'text-red-700 bg-red-100 border border-red-200 p-3 rounded-lg';
      formStatus.classList.remove('hidden');
    } finally {
        if (submitButton) {
            submitButton.disabled = false;
            submitButton.textContent = originalButtonText;
        }
        setTimeout(() => {
            formStatus?.classList.add('hidden');
        }, 5000);
    }
  }
  form?.addEventListener("submit", handleSubmit);
</script>