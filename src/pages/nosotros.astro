---
import Layout from '../layouts/Layout.astro';
import { Image } from 'astro:assets';
import nosotrosHero from '../assets/nosotros-hero-2.jpg';
import nosotrosEquipo from '../assets/nosotros-equipo.jpg';
---

<Layout 
  title="Nosotros - E-GoCars" 
  description="Conoce la filosofía y el equipo detrás de E-GoCars, tu concesionario de confianza."
>
  
  <section class="relative h-screen flex items-center justify-center text-center text-white overflow-hidden">
    <Image 
      src={nosotrosHero} 
      alt="Interior de un auto de lujo" 
      class="absolute inset-0 w-full h-full object-cover -z-10 animate-kenburns"
    />
    <div class="absolute inset-0 bg-gradient-to-t from-gray-900 via-black/70 to-transparent"></div>
    <div class="relative z-10 px-4 animate-fade-in-up">
      <h1 class="text-5xl md:text-7xl font-black tracking-tighter mb-4">
        La Nueva Era Automotriz.
      </h1>
      <p class="text-xl md:text-2xl text-gray-200 max-w-3xl mx-auto font-light">
        En E-GoCars, redefinimos la experiencia de adquirir un vehículo.
      </p>
    </div>
  </section>

  <section class="py-24 bg-gray-900 text-white">
    <div class="max-w-7xl mx-auto px-4 grid md:grid-cols-2 gap-16 items-center">
      <div class="space-y-6">
        <h2 class="text-4xl font-bold tracking-tight">Nuestra Filosofía</h2>
        <p class="text-lg text-gray-300 leading-relaxed">
          Creemos que comprar o vender un auto debe ser una experiencia emocionante y sin complicaciones. Por eso, combinamos la mejor tecnología con un equipo humano experto y apasionado, listo para brindarte una asesoría honesta y soluciones a tu medida.
        </p>
        <p class="text-lg text-gray-300 leading-relaxed">
          Cada auto en nuestro stock es una promesa de calidad, seguridad y rendimiento.
        </p>
      </div>
      <div>
        <Image 
          src={nosotrosEquipo}
          alt="Equipo de E-GoCars"
          class="rounded-2xl shadow-2xl w-full h-full object-cover"
        />
      </div>
    </div>
  </section>

  <section class="py-20 bg-amber-500">
    <div class="max-w-4xl mx-auto px-4 text-center text-black">
      <h2 class="text-3xl md:text-4xl font-bold mb-6">¿Listo para dar el siguiente paso?</h2>
      <p class="text-xl mb-8">Nuestro equipo está aquí para ayudarte a encontrar tu próximo auto o a gestionar la venta del actual.</p>
      <a href="/contacto" class="inline-block bg-gray-900 text-white font-bold py-4 px-8 rounded-lg text-lg hover:bg-black transition-colors">
        Habla con un Especialista
      </a>
    </div>
  </section>

</Layout>

<style>
  @keyframes kenburns {
    0% {
      transform: scale(1.1) translate(0, 0);
    }
    100% {
      transform: scale(1) translate(0, 0);
    }
  }
  .animate-kenburns {
    animation: kenburns 10s ease-out forwards;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  .animate-fade-in-up {
    animation: fadeInUp 1s ease-out forwards;
  }
</style>